# 📐 System Architecture — VoiceActions AI

> *Every box, arrow, model call, and data store labelled.*

---

```mermaid
flowchart TD
    subgraph Client ["Client (Frontend — Next.js 16)"]
        UI["User Interface (Split Hero Layout)"]
        Record["Browser MediaRecorder + Web Speech API"]
        StreamUI["NDJSON Stream Consumer & Stepper"]
    end

    subgraph Backend ["Server (Backend — FastAPI)"]
        API["FastAPI Orchestrator Router (/api/process)"]
        
        subgraph Agent ["Hand-Rolled Agent Pipeline"]
            Step1["1. Transcribe Step"]
            Step2["2. Analyze Step"]
            Step3["3. Verify Step"]
            Step4["4. Store Step"]
        end
    end

    subgraph AI_Services ["External AI Model Providers"]
        Whisper["Groq Whisper (whisper-large-v3)\n[Audio -> Text]"]
        Gemini["Google Gemini (gemini-3.6-flash)\n[Action Extraction & Conflict Detection]"]
        Qwen["Groq Qwen (qwen/qwen3.6-27b)\n[Verification Audit & Confidence Score]"]
        Embed["Gemini Embeddings (text-embedding-004)\n[768-dim Vector Embeddings]"]
    end

    subgraph DataStore ["Database & Persistence"]
        DB[(Supabase PostgreSQL)]
        VectorDB[("pgvector (Cosine Similarity Index)")]
        Storage[("Supabase Audio Storage Bucket")]
    end

    %% Flow Connections
    Record -->|"Audio WebM Blob"| API
    UI -->|"Raw Text Input"| API
    API --> Step1
    
    Step1 -->|"HTTP / Audio File"| Whisper
    Whisper -->|"Transcript Text"| Step2
    
    Step2 -->|"Prompt + Transcript"| Gemini
    Gemini -->|"JSON: Actions, Conflicts, Ambiguities"| Step3
    
    Step3 -->|"Prompt + First AI Output"| Qwen
    Qwen -->|"Audit Report & Score Adjustment"| Step4
    
    Step4 -->|"Save Note & Actions"| DB
    Step4 -->|"Action Text"| Embed
    Embed -->|"768-dim Vectors"| VectorDB
    
    Step4 -->|"Stream NDJSON Events"| StreamUI
    StreamUI -->|"Render UI Cards & Confidence Gauge"| UI
```

---

## 🔍 Layer-by-Layer Data Flow

### 1. Input Layer (Client)
- **Voice Mode:** Captures WebM audio via `navigator.mediaDevices.getUserMedia()`. On-device **Web Speech API** provides real-time speech preview.
- **Text & Document Mode:** Accepts raw meeting notes or text files directly.

### 2. Orchestration Layer (FastAPI Backend)
- Hand-rolled asynchronous agent loop (NO LangChain overhead).
- Manages **retries** (exponential backoff), **fallback chains** (Gemini ↔ Groq), and **decision trace logging**.
- Streams step-by-step progress to the frontend using `application/x-ndjson`.

### 3. Model Pipeline (Two-Model Cooperation)
- **Step 1 — Transcribe:** `Groq Whisper-large-v3` transcribes audio (handles Hindi-English code mixing).
- **Step 2 — Analyze:** `Google Gemini 3.6 Flash` extracts structured action items, severe conflicts, and missing details (ambiguities).
- **Step 3 — Verify Audit:** `Groq Qwen 27B` audits Gemini's output for false conflicts or missed items and adjusts final confidence.

### 4. Data & Retrieval Layer (Supabase + pgvector)
- **PostgreSQL Database:** Stores voice notes, structured action cards, and conflict logs.
- **pgvector:** Stores 768-dimensional embeddings generated by `text-embedding-004` for semantic search over past team actions.
